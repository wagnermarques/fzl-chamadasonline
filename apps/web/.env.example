# Leave unset for local dev (requests go through the Vite proxy to
# http://localhost:3333, see vite.config.ts). Set this in production builds
# (GitHub Actions repo variable API_URL) to the hosted API's public URL.
VITE_API_URL=
